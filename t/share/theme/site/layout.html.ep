<!DOCTYPE html>
<html>
    <head>
        <title><%= $site->title %></title>
    </head>
    <body>
        % for my $nav ( @{ $site->nav->{ main } } ) {
            <a href="<%= $nav->{href} %>"><%= $nav->{title} %></a>
        % }
        <%= $content %>
    </body>
</html>
